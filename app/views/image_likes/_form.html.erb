<%= form_for(image_like) do |f| %>
  <% if image_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_like.errors.count, "error") %> prohibited this image_like from being saved:</h2>

      <ul>
      <% image_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :image_id %>
    <%= f.number_field :image_id %>
  </div>

  <div class="field">
    <%= f.label :like %>
    <%= f.number_field :like %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
